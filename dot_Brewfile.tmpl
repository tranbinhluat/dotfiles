# Homebrew Bundle file — installed packages for both macOS and Linux (Linuxbrew).
# Managed by chezmoi as ~/.Brewfile and applied via `brew bundle --file=~/.Brewfile`
# in run_onchange_install_packages.sh.tmpl.
#
# Add/remove formulae here; the install script picks up changes automatically.

brew "git"
brew "zsh"
brew "k9s"
brew "kubectl"
brew "pyenv"
brew "pnpm"
brew "opencode"
brew "jq"
brew "tree"
brew "wget"
brew "ffmpeg" # Lib for gift
brew "just"
brew "cmake"
brew "cloudflared"
brew "uv"
brew "node"
brew "age" # Simple, modern, secure file encryption


{{- if eq .chezmoi.os "darwin" }}

# macOS CLI-only Docker environment (Colima runtime + Docker CLI & plugins)
brew "colima"
brew "docker"
brew "docker-compose"
brew "docker-buildx"
brew "tailscale" # tailscale client only for macOS. For host(linux), install in system level

# set default app folder for macos cask
cask_args appdir: "/opt/app"

cask "iterm2" # terminal

cask "microsoft-remote-desktop" # remote dekstop
{{- end }}


{{- if eq .chezmoi.os "linux" }}

{{- end}}

